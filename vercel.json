{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "flutter",
  "buildCommand": "flutter pub get && flutter build web --release --base-href /",
  "outputDirectory": "build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}